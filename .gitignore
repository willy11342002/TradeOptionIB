# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# kgisuperpy 舊版殘留在工作目錄的錯誤碼對照檔，已不再使用
errMsg.ini

# 所有「記憶狀態」的本機檔案 (連線設定/版面配置/查詢條件/委託紀錄等)統一
# 放這裡，見 app/paths.py 的 PREF_DIR 說明——一個資料夾規則就夠，不用每
# 加一個新的偏好檔案就手動補一行。
pref/

# API 金鑰設定，不進版控 (參考 .env.example 填自己的 key)——唯一不搬進
# pref/ 的例外，見 app/paths.py 的說明。
.env

# gce/ 部署腳本讀的 GCP project id、IBKR 帳密等設定，只留 .example 範
# 本進版控，實際檔案不要 commit。
gce/.env

# reqScannerParametersAsync() 的原始 XML 全量 dump (~1.8MB)，只是產生
# app/resources/scan_filters.json / scan_types.json 這兩份精簡翻譯資料
# 用的一次性素材 (scripts/gen_scanner_catalog.py)，app 本身不吃這個檔
# 案，不需要進版控。
scanner_parameters.xml

# app/services/app_logging.py 寫的錯誤/崩潰紀錄，本機除錯用，不進版控。
logs/
