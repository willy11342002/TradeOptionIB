# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# kgisuperpy 舊版殘留在工作目錄的錯誤碼對照檔，已不再使用
errMsg.ini

# 所有「記憶狀態」的本機檔案 (連線設定/版面配置/查詢條件/委託紀錄等)統一
# 放這裡，見 app/paths.py 的 PREF_DIR 說明——一個資料夾規則就夠，不用每
# 加一個新的偏好檔案就手動補一行。
pref/

# API 金鑰設定，不進版控 (參考 .env.example 填自己的 key)——唯一不搬進
# pref/ 的例外，見 app/paths.py 的說明。
.env

# gce/ 底下部署到 VM 用的 IBC config.ini 含帳密，只留 .example 範本進
# 版控，實際檔案(config.live.ini/config.paper.ini)不要 commit。
gce/ibc/config.live.ini
gce/ibc/config.paper.ini
