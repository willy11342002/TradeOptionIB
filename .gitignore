# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# 群益官方範例/COM 元件安裝包，體積大且是廠商原始檔案，不用進版控。
# vendor/capital_api/ 底下的 SKCOM.dll 等執行期需要的 DLL 是從這裡複製過去
# 的，同樣不進版控 (二進位檔案不適合放 git 歷史)——這台機器已經有了，換一
# 台機器要重新把這個資料夾底下的 元件/x64 內容複製一份到 vendor/capital_api/x64。
CapitalAPI_2.13.59_PythonExample/
vendor/capital_api/

# 加密後的登入帳密快取 (DPAPI 二進位，僅本機可解密)
.capital_credentials.bin
# 已改用群益 API，這個是舊的凱基帳密快取檔，本機可能還留著舊檔，一樣不能進版控
.kgi_credentials.bin

# kgisuperpy 舊版殘留在工作目錄的錯誤碼對照檔，已不再使用
errMsg.ini

# 本機使用者偏好設定 (深色模式等)
.theme_pref.json

# 本機 dock 版面配置存檔 (視窗幾何座標，機器相依)
.layout_pref.json

# 本機下單匣/成交回報保存檔 (重開視窗仍要看到委託紀錄，機器相依)
.order_book_pref.json

# 本機未平倉部位手動分組/標籤存檔
.position_groups_pref.json

# API 金鑰設定，不進版控 (參考 .env.example 填自己的 key)
.env

# 開倉判斷紀錄，本機執行期產生
open_position_history.jsonl

# 期交所選擇權波動率指數本地增量快取 (本機執行期產生/累積)
data/taiwan_option_vix.csv

# FinMind 加權指數/台指期價格本地增量快取 (本機執行期產生/累積)
data/taiex_price_history.csv
data/tx_futures_price_history.csv
